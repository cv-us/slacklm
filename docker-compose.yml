services:
  slacklm:
    build: .
    restart: always
    env_file:
      - .env
    volumes:
      - ./storage_state.json:/root/.notebooklm/profiles/default/storage_state.json:ro
      - ./config/channels.yaml:/app/config/channels.yaml:ro
